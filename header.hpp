#include <string>
#include <fstream>
#include <sstream>
#include <iostream>
#include "glad/glad.h"
#include <GLFW/glfw3.h>

GLuint createShaderProgram(const char* vertexPath, const char* fragmentPath);